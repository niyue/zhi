$('#questions_count').html("<%= j(render partial: 'questions_count') %>");
$('#<%= @question.question_type + @question.question_id.to_s %>')
    .html("<%= j(render partial: 'remove_button', locals: { questionship: @question, exam: @exam }) %>");
