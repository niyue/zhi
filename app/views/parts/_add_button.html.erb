<%= form_for([exam, Part.new], remote: true) do |f| %>
    <%= f.hidden_field :question_id, value: question.id %>
    <%= f.hidden_field :question_type, value: question.class.name %>
    <button type="submit" class="btn btn-default btn-success">添加至試卷</button>
<% end %>