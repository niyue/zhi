<p id="notice"><%= notice %></p>
<div class="row">
  <div class="col-md-11"><h1>問答題列表</h1></div><!--
  --><div class="col-md-1 top-padding-buffer">
      <%= link_to '新的問題', new_essay_path %>
    </div>
</div>
<% if params['tags'] %>
<div class="row">
  <div class="col-md-11">
    標籤:
    <% for tag in params['tags'].split(',') %>
      <%= link_to tag, essays_path(tags: tag) %> | 
    <% end %>
  </div>
</div>
<% end %>

<table class="table table-striped table-bordered top-buffer">
  <thead>
    <tr>
      <th>序號</th>
      <th>名稱</th>
      <th>問題</th>
      <th colspan="2" width="10%">操作</th>
    </tr>
  </thead>

  <tbody>
    <% @essays.each do |essay| %>
      <tr>
        <td><%= link_to essay.id, essay %></td>
        <td><%= link_to essay.name, essay %></td>
        <td><%=raw essay.description %></td>
        <td><%= link_to '編輯', edit_essay_path(essay) %></td>
        <td><%= link_to '刪除', essay, method: :delete, data: { confirm: '確定?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

