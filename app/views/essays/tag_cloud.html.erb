<div class="tag_cloud">
    <% tag_cloud(@tags, %w(tag_small tag_normal tag_medium tag_big)) do |tag, css_class| %>
        <%= link_to tag.name, essays_path(tags: tag.name), :class => css_class %> |
    <% end %>
</div>